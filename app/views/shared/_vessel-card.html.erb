<%= link_to vessel_path(vessel) do %>
<div class="col col-xs-12 col-sm-6 col-md-4">
  <div class="vessel-card" style="background-image: linear-gradient(-225deg, rgba(0,0,0,0.5) 0%, rgba(0,0,0,0.5) 50%), url(<%= vessel.photo.url(:card) %>)";>
    <div class="vessel-card-face">
      <h2><%= vessel.name %></h2>
      <p><%= vessel.price %> €</p>
    </div>
    <div class="vessel-card-back">
      <p><i>"<%= vessel.description %>"</i></p>
    </div>
  </div>
</div>
<% end %>
