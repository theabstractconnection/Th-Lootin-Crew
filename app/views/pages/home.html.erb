<%= render 'shared/banner' %>

<div class="container-fluid">
  <h1>Our best vessels!</h1>
  <div class="row">
    <% @vessels.each do |vessel| %>
      <%= render 'shared/vessel-card', vessel: vessel %>
    <% end %>
  </div>
</div>
